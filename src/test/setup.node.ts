// Node.js test setup - no DOM-related imports
// Add Node.js specific mocks and setup here

// Example: Mock environment variables
// process.env.PAYLOAD_SECRET = 'test-secret'

// Example: Setup database connection for tests
// beforeAll(async () => { ... })
// afterAll(async () => { ... })
